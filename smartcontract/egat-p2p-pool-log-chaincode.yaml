apiVersion: v1
kind: Service
metadata:
  name: egat-pool-chaincode-002
  namespace: hyperledger
spec:
  ports:
  - name: grpc
    port: 3000
    targetPort: 3000
  selector:
    app: egat-pool-chaincode-002
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: egat-pool-chaincode-002
  name: egat-pool-chaincode-002
  namespace: hyperledger
spec:
  selector:
    matchLabels:
      app: egat-pool-chaincode-002
  template:
    metadata:
      labels:
        app: egat-pool-chaincode-002
    spec:
      containers:
      - name: egat-pool-chaincode-002
        image: phwuts/egat-p2p-pool-log-chaincode:0.0.2
        imagePullPolicy: Always
        env:
        - name: CORE_CHAINCODE_ID
          value: 'egat-pool_0.0.1:xxxxxxx'
        - name: CORE_CHAINCODE_ADDRESS
          value: 0.0.0.0:3000
        ports:
        - containerPort: 3000
        resources: {}
      dnsPolicy: ClusterFirst