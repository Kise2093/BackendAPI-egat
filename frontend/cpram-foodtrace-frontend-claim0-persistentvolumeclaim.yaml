apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: egat-p2p-webadmin-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  storageClassName: rook-cephfs
status: {}
